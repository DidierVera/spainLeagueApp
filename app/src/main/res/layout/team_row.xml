<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_margin="12dp"
    android:layout_height="150dp">

    <TextView
        android:id="@+id/tvTeamName"
        android:textSize="24dp"
        android:textStyle="bold"
        android:layout_width="0dp"
        android:layout_marginTop="12dp"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toLeftOf="@id/ivTeamBadge"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Barcelona FC" />

    <TextView
        android:id="@+id/tvStadium"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="20dp"
        android:textStyle="italic"
        android:layout_marginBottom="4dp"
        app:layout_constraintRight_toLeftOf="@id/ivTeamBadge"
        app:layout_constraintBottom_toTopOf="@id/vwSeparator"
        app:layout_constraintLeft_toLeftOf="parent"
        tools:text="Cap Nou Futbol Club stadium" />

    <ImageView
        android:id="@+id/ivTeamBadge"
        android:layout_width="140dp"
        android:layout_height="140dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/vwSeparator"
        app:layout_constraintTop_toTopOf="parent" />
    <View
        android:id="@+id/vwSeparator"
        android:layout_width="match_parent"
        android:layout_height="0.92dp"
        android:layout_marginTop="12dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@color/lightGray" />

</androidx.constraintlayout.widget.ConstraintLayout>